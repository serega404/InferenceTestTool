# InferenceTestTool

## Настройка через переменные окружения

Скопируйте пример и при необходимости измените значения:

```bash
cp .env.example .env
```

Скрипт читает переменные окружения напрямую (через `os.getenv`).
Если используете `.env`, экспортируйте переменные в shell перед запуском.
